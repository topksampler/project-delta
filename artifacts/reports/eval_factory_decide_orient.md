# Orient — what’s left for DELTA DECIDE (post factory-FT stop)

**Date:** 2026-07-29

## Invariant

```text
Factory board taught diet + matched-context ceilings.
DECIDE still lacks an automatic policy that picks diet vs retrieval vs abstain.
FT stays stopped until explicit reopen.
```

## Map (human words)

| layer | status | owns |
|---|---|---|
| **SENSE** | partial | corpus/version diff exists; factory claims; profile wheel separate |
| **BUILD** | partial | sealed factory banks; evidence maps; diff path index |
| **DECIDE** | **gap** | no policy that says “delta-diet vs oracle debug vs deployable retrieve vs stop” |
| **VERIFY** | partial | drift_type scoreboard; two-scoreboard discipline |
| **FT** | **stopped** | factory + profile past v7 |

## What this arc settled

1. Pooled exact ≠ transition competence.
2. Diet overweighting deltas is a real closed-book lever.
3. Oracle RAG is ceiling/debug, not deploy.
4. Naive bridges (file expand, allowlist BM25, symbol_diff) do not close the gap.

## What’s still open for DECIDE

1. **Feature → action table** — **drafted** in `eval_factory_decide_policy.md` (diet vs diff-hunk vs oracle vs stop).
2. **Deployable join** — **partial win**: diff-hunk *corpus* (not allowlist) lifts `changed` on two banks; entity×hunk router / hybrid with stable corpus still open.
3. **Cost model** — sketched as ladder in policy doc; not yet automated thresholds.
4. **Promotion rules** — stated in policy; not yet code in VERIFY.
5. **Scoreboard glue** — stated; not yet enforced in dispatch.

## What can die

- More factory LoRA experiments without a DECIDE question
- `diff_bm25` / `symbol_expand` as defaults

## What must survive

- Stop-FT seal
- Diet recipe + drift tables
- Diff allowlist as a *feature*, not a finished retriever

## Command

Read: `artifacts/reports/eval_factory_ft_stop_freeze.md`,
`eval_factory_diff_prior.md`, `docs/blog/delta-part-5-diet-oracle-bridge.md`.
